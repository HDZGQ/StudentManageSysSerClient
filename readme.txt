环境搭建步骤：
1.先装好mysql和vs。mysql推荐使用5.0（安装好后可以界面配置）以上的，vs2012以上即可。
2.按顺序执行”msyql文件“文件夹下的三个bat批处理文件：清空数据库.bat，创建并初始化数据库.bat，创建存储过程.bat。
3.打开服务端和客户端解决方案，根据自己设置的数据库账号密码端口号修改服务端代码配置，都编译好（debug模式即可）。先运行服务端，再运行客户端。客户端可以运行多个，多次双击在客户端解决方案目录下的debug目录里面的exe文件即可。
4.在客户端登录或者注册到系统后，就可以各种小风骚操作了。
